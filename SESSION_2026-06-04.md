# Session Notes — 2026-06-04

## Context
Review of co-admin feedback + code audit against actual source files. Findings listed below with file references and root-cause analysis.

---

## Admin PIN — Invalid PIN silently fails (#1)

**Status:** 🟢 Done  
**Commits:** `28c8777`, `e48a2a8` (retry logic)  
**Files changed:**
- `apps/web/src/lib/api.ts` — added `ADMIN_PIN_INVALID` handler: clears `adminPin` from auth store, re-dispatches `admin:pin-required`
- `apps/web/src/components/admin-pin-modal.tsx` — listens for `admin:pin-invalid` event and displays server error message

**Fix applied:** Wrong PIN now immediately triggers the modal to re-open with a visible error instead of silently failing.

---

## Notification routing — deposits/transactions go to `/dashboard`

**Status:** 🟢 Done  
**Commit:** `28c8777`  
**File changed:** `apps/web/src/components/notification-bell.tsx`

**Fix applied:** Added `CREDIT_EARNED`, `CREDIT_SPENT`, `DEPOSIT_COMPLETED`, `DEPOSIT_FAILED`, `WITHDRAWAL_COMPLETED`, `WITHDRAWAL_FAILED` → `/wallet` routing. Also added `depositId` to `AppNotification.data` type.

---

## Expand deposit info in `/wallet` (#3)

**Status:** 🟢 Done  
**Commit:** `c066ffd`  
**File changed:** `apps/web/src/app/(dashboard)/wallet/page.tsx`

**Fix applied:** Added expandable detail panel to each deposit history row:
- Chevron toggle on every row
- Expanded panel shows: deposit ID, payment reference (copyable), status, method, currency, amount, exchange rate, credits breakdown, timestamps, package info
- Raw `gatewayData` rendered as formatted JSON block
- `DetailItem` helper component with copy-to-clipboard support

---

## User cancel deposit + admin/finances sync (#4, #11)

**Status:** 🟢 Done  
**Commit:** `28c8777`  
**Files changed:**
- `apps/web/src/app/(dashboard)/wallet/page.tsx` — `cancelDepositMutation.onSuccess` now unconditionally calls `resetDeposit()` + uses `queryClient.refetchQueries` instead of `invalidateQueries`
- `apps/web/src/app/(admin)/admin/finances/page.tsx` — `canReview` now only allows review for `PENDING` or `PROCESSING` deposits

**Fix applied:** Cancelled deposits immediately clear the UI form and force immediate refetch of deposit history. Admin review button is disabled for terminal statuses (CANCELLED, FAILED, COMPLETED, REFUNDED).

---

## Auto-cancel or gracefully retry on API errors (#5)

**Status:** 🟢 Done  
**Commit:** `e48a2a8`  
**File changed:** `apps/web/src/lib/api.ts`

**Fix applied:** Added response interceptor retry logic:
- Retries up to 3 times for network errors (no response) and 5xx server errors
- Exponential backoff: 1s, 2s, 4s delays
- Skips retries for 4xx client errors and admin-specific 403s

---

## Email sender address: `no-reply@engganyo.com` (#6)

**Status:** 🟢 Done  
**Commit:** `5491329`  
**File changed:** `apps/api/src/config/email.config.ts:10`

**Fix applied:** Default fallback changed from `'noreply@engganyo.com'` → `'no-reply@engganyo.com'` (with hyphen) for brand consistency.

---

## Platform logos in campaign dropdown (#7)

**Status:** 🟢 Done  
**Commit:** `033350d`  
**Files changed:**
- `apps/web/src/components/platform-icon.tsx` — new inline SVG brand icons for all platforms (YouTube, TikTok, Instagram, Twitter/X, Facebook, Twitch, Spotify, Telegram, Discord, TrustPilot, Google)
- `apps/web/src/components/platform-select.tsx` — new custom accessible dropdown with per-platform color + icon
- `apps/web/src/app/(dashboard)/campaigns/page.tsx` — replaced native `<select>` with `<PlatformSelect>`

**Fix applied:** Official brand SVG icons rendered in both the trigger button and each dropdown item, with platform-appropriate colors.

---

## `/admin/revenue` Danger Zone warning (#8)

**Status:** 🟢 Done  
**Commit:** `4744b75`  
**File changed:** `apps/web/src/app/(admin)/admin/revenue/page.tsx`

**Fix applied:** Added red-bordered danger zone card at the bottom of the page:
- Warning that credit revenue is **not fiat revenue** (do not use for tax/accounting)
- Force Refresh button to invalidate + refetch revenue data

---

## Deposit notifications not wiped on database reset (#9)

**Status:** 🟢 Done  
**Commit:** `5491329`  
**File changed:** `apps/api/src/modules/admin/admin.service.ts`

**Fix applied:** Added `await tx.notification.deleteMany({ where: { userId: { in: keptIds } } });` inside the `resetDatabase` transaction before resetting kept admin accounts. Old deposit notifications no longer persist after reset.

---

## Inline cancel modal — already done (#10)

**Status:** 🟢 Done  
**File:** `apps/web/src/app/(dashboard)/wallet/page.tsx`

**Details:** The cancel deposit flow already uses a themed inline modal (not browser `alert()`). No changes needed.

---

## Cancelled deposit not updating Deposit Credit tab until refresh (#11)

**Status:** 🟢 Done  
**Commit:** `28c8777`  
**File changed:** `apps/web/src/app/(dashboard)/wallet/page.tsx`

**Fix applied:** `cancelDepositMutation.onSuccess` now unconditionally calls `resetDeposit()` and uses `queryClient.refetchQueries()` instead of `invalidateQueries()` for immediate UI refresh.

---

## Admin 400 error when marking completed after cancel (#12)

**Status:** 🟢 Done  
**Commit:** `28c8777`  
**File changed:** `apps/web/src/app/(admin)/admin/finances/page.tsx`

**Fix applied:** `canReview` logic changed to only allow review for `PENDING` or `PROCESSING` deposits. Terminal statuses (CANCELLED, FAILED, COMPLETED, REFUNDED) show "—" instead of a review button.

---

## `/admin/revenue` — Tracks credits, not actual cash flow (#13)

**Status:** 🟢 Done  
**Commit:** `0d04ec4`  
**Files changed:**
- `apps/web/src/app/(admin)/admin/revenue/page.tsx` — renamed to "Platform Earnings", added dual-view layout
- `apps/api/src/modules/admin/admin.service.ts` — `getRevenueSummary` now queries completed deposits for cash flow data
- `apps/api/prisma/schema.prisma` — added `onDelete: Cascade` to `PlatformRevenue -> Campaign` relation

**Fix applied:**
1. Page renamed to "Platform Earnings" with clear credit vs cash distinction
2. Backend now returns `cashFlow` data from `DepositStatus.COMPLETED` records grouped by date/method/currency
3. Frontend displays two sections: Credit Revenue (campaign fees) + Cash Flow (real deposits)
4. `resetDatabase()` now explicitly deletes `platformRevenue` records before campaigns

---

## Recommended Fix Order — ALL COMPLETED ✅

| Priority | Item | Status |
|----------|------|--------|
| 1 | #1 Admin PIN invalid handling | ✅ Done |
| 2 | #2 Notification routing to `/wallet` | ✅ Done |
| 3 | #12 Admin UI guard for cancelled deposits | ✅ Done |
| 4 | #11 Cancel refresh bug | ✅ Done |
| 5 | #3 Expand deposit info | ✅ Done |
| 6 | #6 `no-reply@` email | ✅ Done |
| 7 | #9 Notifications wiped on reset | ✅ Done |
| 8 | #4 User cancel deposit + admin sync | ✅ Done |
| 9 | #7 Platform logos | ✅ Done |
| 10 | #8 Revenue Danger Zone | ✅ Done |
| 11 | #10 Inline cancel modal | ✅ Already done |
| 12 | #5 API retry/auto-cancel | ✅ Done |
| 13 | #13 `/admin/revenue` data model confusion | ✅ Done |

**All 13 items from the co-admin feedback have been addressed and pushed to `main`.**
