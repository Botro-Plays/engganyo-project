import { Module } from '@nestjs/common';

/**
 * AuthModule — Phase 2
 * Handles: register, login, email verification, JWT, refresh tokens, password reset
 */
@Module({
  imports: [],
  controllers: [],
  providers: [],
  exports: [],
})
export class AuthModule {}
